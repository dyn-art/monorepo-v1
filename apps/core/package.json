{
  "name": "@pda/core",
  "version": "0.0.0",
  "description": "Core Backend",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "shx rm -rf dist && tsc",
    "start:dev": "nodemon --config ./nodemon.json",
    "lint": "eslint --cache \"**/*.{js,ts}\"",
    "install:clean": "shx rm -rf dist && shx rm -rf node_modules && pnpm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "@bennodev19",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@pda/tsconfig": "workspace:*",
    "@types/node": "^18.15.1",
    "eslint": "^7.32.0",
    "eslint-config-pda": "workspace:*",
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1",
    "typescript": "^4.5.2"
  }
}
