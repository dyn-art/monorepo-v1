{
  "name": "@pda/render-worker",
  "version": "0.0.0",
  "description": "Renderer Backend",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "shx rm -rf dist && tsc",
    "start:dev": "nodemon --config ./nodemon.json",
    "lint": "eslint --cache \"**/*.{js,ts,tsx}\"",
    "clean": "shx rm -rf dist && shx rm -rf node_modules && shx -rf .turbo",
    "install:clean": "pnpm run clean && pnpm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "@bennodev19",
  "license": "MIT",
  "dependencies": {
    "@pda/logger": "workspace:*",
    "@pda/poster-renderer": "workspace:*",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@pda/tsconfig": "workspace:*",
    "@types/node": "^18.15.1",
    "eslint": "^7.32.0",
    "eslint-config-pda-base": "workspace:*",
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1",
    "typescript": "^4.5.2"
  }
}
