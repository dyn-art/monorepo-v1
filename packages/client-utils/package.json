{
  "name": "@pda/client-utils",
  "version": "0.0.0",
  "description": "Utility library to communicate with the Core",
  "private": true,
  "source": "./src/index.ts",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "shx rm -rf dist && npx esno ../../scripts/build-ts-package.ts --prod",
    "start:dev": "tsc -w",
    "lint": "eslint --cache \"**/*.{js,ts}\"",
    "clean": "shx rm -rf dist && shx rm -rf node_modules && shx rm -rf .turbo",
    "install:clean": "pnpm run clean && pnpm install",
    "test": "jest"
  },
  "keywords": [],
  "author": "@bennodev19",
  "license": "MIT",
  "dependencies": {
    "@pda/logger": "workspace:*",
    "@pda/utils": "workspace:*",
    "cross-fetch": "^3.1.6",
    "openapi-fetch": "^0.6.0"
  },
  "devDependencies": {
    "@pda/tsconfig": "workspace:*",
    "eslint": "^8.38.0",
    "eslint-config-pda-base": "workspace:*",
    "typescript": "^4.9.5"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
