import { TUIEventMeta } from '../UIEvent';
import domContentLoadedEvent from './dom-content-loaded.event';

const defaultEvents: TUIEventMeta[] = [domContentLoadedEvent];

export default defaultEvents;
