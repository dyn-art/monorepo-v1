{
  "name": "@pda/maptiler-api",
  "version": "0.0.0",
  "description": "Utility library to work with Maps and communicate with Maptiler",
  "private": true,
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "shx rm -rf dist && tsc",
    "start:dev": "tsc -w",
    "lint": "eslint --cache \"**/*.{js,ts}\"",
    "clean": "shx rm -rf dist && shx rm -rf node_modules && shx -rf .turbo",
    "install:clean": "pnpm run clean && pnpm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "@bennodev19",
  "license": "MIT",
  "devDependencies": {
    "@pda/tsconfig": "workspace:*",
    "axios": "^1.3.6",
    "eslint": "^8.38.0",
    "eslint-config-pda-base": "workspace:*",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "axios": "^1.3.4"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
