{
  "name": "@pda/etsy",
  "version": "0.0.0",
  "description": "Utility library to communicate with Etsy",
  "private": true,
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "shx rm -rf dist && tsc",
    "start:dev": "tsc -w",
    "lint": "eslint --cache \"**/*.{js,ts}\"",
    "clean": "shx rm -rf dist && shx rm -rf node_modules && shx -rf .turbo",
    "install:clean": "pnpm run clean && pnpm install",
    "test": "jest"
  },
  "keywords": [],
  "author": "@bennodev19",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@pda/jest-presets": "workspace:*",
    "@pda/tsconfig": "workspace:*",
    "@types/jest": "^29.4.1",
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "eslint": "^7.32.0",
    "eslint-config-pda-base": "workspace:*",
    "jest": "^29.5.0",
    "typescript": "^4.5.2"
  },
  "peerDependencies": {
    "axios": "^1.3.4"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
